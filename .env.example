# Optional: enables prompt-based generation and HTML/CSS-to-React conversion.
# Leave this unset if you only want to browse and manage the local component library.
GEMINI_API_KEY="your_gemini_api_key"
